# Local dev only — copy to .env. From the Clerk dashboard → API Keys
# (Development instance). Publishable key is safe to expose client-side.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
# iOS simulator can reach the host as localhost; Android emulator needs
# 10.0.2.2 instead; a physical device needs the host's LAN IP or a tunnel.
EXPO_PUBLIC_API_URL=http://localhost:3000
