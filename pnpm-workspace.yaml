packages:
  - 'apps/*'
  - 'packages/*'

# esbuild's postinstall just links its prebuilt platform binary — pnpm 10
# blocks all install scripts by default, so this needs an explicit allow.
onlyBuiltDependencies:
  - esbuild
