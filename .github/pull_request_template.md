## Summary

<!-- What does this change, and why? -->

## Test plan

<!-- How was this verified? e.g. `pnpm lint`, `pnpm typecheck`, manual check against the local Docker stack -->

- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
- [ ] `pnpm build`
- [ ] Manually verified against the local dev stack (`pnpm docker:up`)
